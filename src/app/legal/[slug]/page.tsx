import { notFound } from "next/navigation";

const pages: Record<string, { title: string; text: string; sections: string[] }> = {
  terms: { title: "Пользовательское соглашение", text: "Документ определяет правила использования Monetrix — сервиса для анализа личных и бизнес-финансов.", sections: ["Пользователь отвечает за достоверность загружаемых данных и сохранность доступа к аккаунту.", "Monetrix показывает расчёты и рекомендации в информационных целях. Решения о кредитах, инвестициях и налогах принимает пользователь после проверки условий у поставщика.", "Запрещены попытки получить доступ к чужим данным, обходить ограничения и загружать вредоносные файлы."] },
  privacy: { title: "Политика конфиденциальности", text: "Мы используем только те данные, которые нужны для выбранных функций кабинета и подсказок Кэшика.", sections: ["При регистрации мы запрашиваем согласие на обработку данных. В настройках можно отключить отдельные категории, отозвать согласие, экспортировать или удалить данные.", "Данные не продаются и не передаются экспертам или партнёрам без отдельного действия пользователя. Доступ защищается авторизацией, cookie-сессиями и ограничениями базы данных.", "Кэшик работает с переданным контекстом для ответа на запрос. Он не получает доступ к данным, для которых выключено согласие, и не совершает финансовые операции от имени пользователя."] },
  offer: { title: "Публичная оферта", text: "Использование Monetrix означает принятие условий предоставления информационного сервиса.", sections: ["Сервис предоставляет доступ к функциональности после регистрации и действует по принципу разумной доступности.", "Банковские продукты, ставки и рыночные показатели сопровождаются ссылками на первоисточники и датой обновления; перед действием проверяйте актуальные условия.", "Актуальная версия документа и реквизиты оператора должны быть опубликованы до запуска сервиса в продуктивной среде."] },
};

export default async function LegalPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const page = pages[slug];
  if (!page) notFound();
  return <main className="mx-auto flex min-h-[60vh] max-w-3xl flex-col gap-6 px-6 py-16"><p className="text-sm font-semibold text-primary">ООО «МОНЕТРИКС»</p><h1 className="text-3xl font-bold">{page.title}</h1><p className="leading-7 text-muted-foreground">{page.text}</p><div className="flex flex-col gap-4">{page.sections.map((section) => <p key={section} className="leading-7 text-muted-foreground">{section}</p>)}</div><p className="text-sm text-muted-foreground">Версия документа: 1.0 · требуется юридическая проверка перед публикацией.</p></main>;
}
